.vscode/*
!.vscode/extensions.json
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Secrets: the real .env is not tracked in generated projects (manage values in
# your deploy platform / a local .env). .env.example is the tracked reference.
# Root-anchored so it does not also match frontend/.env (which is tracked).
/.env

# Local envs, build output and caches
.venv/
/frontend/dist/
/backend/htmlcov/
__pycache__/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.idea/

# Tooling / assistant artifacts
.playwright-mcp/
.claude/
